# Base URL of the backend API. Leave empty for same-origin (Vite dev proxy handles /v1 and /auth).
# In production builds (GitHub Pages), point at the Cloud Run URL, e.g.:
#   VITE_API_BASE=https://ai-dispatcher-xxxxxxxxxx-uc.a.run.app
VITE_API_BASE=
